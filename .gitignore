# --- Artefatos de trabalho do Claude / planejamento (não versionar) ---
CLAUDE.md
claude.md
plano.md
.claude/
memoria/
memory/

# --- R ---
.Rproj.user/
.Rhistory
.RData
.Ruserdata
*.Rproj
.Renviron

# Pacote R compilado / build (em qualquer profundidade, inclui src/vendor/)
src/**/*.o
src/**/*.so
src/**/*.dll
src/**/*.dylib
src/*.o
src/*.so
src/*.dll
src/*.dylib
src/Makevars.local
*.tar.gz
..Rcheck/
*.Rcheck/
revdep/

# cpp11 / Rcpp gerados localmente (mantemos os commitados, ignoramos lixo de build)
src/RcppExports.o
symbols.rds

# Vendoring temporário do upstream PyVRP
src/vendor/.cache/
tools/pyvrp-upstream/

# Instâncias de benchmark baixadas (dados de terceiros; o driver re-baixa)
tools/benchmark/*.vrp
tools/benchmark/*.sol

# --- Sistema / editores ---
.DS_Store
.vscode/
.idea/
*.swp
