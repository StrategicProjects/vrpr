% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{add_shipments}
\alias{add_shipments}
\title{Add shipments (pickup and delivery pairs) to the model}
\usage{
add_shipments(model, data)
}
\arguments{
\item{model}{A \code{vrpr_model}.}

\item{data}{A tibble/data.frame with at least the columns \code{pickup_x},
\code{pickup_y}, \code{delivery_x} and \code{delivery_y}. Optional columns: \code{amount}
(load carried between pickup and delivery), \code{pickup_tw_early},
\code{pickup_tw_late}, \code{pickup_service}, \code{delivery_tw_early},
\code{delivery_tw_late}, \code{delivery_service}, \code{prize} and \code{required}.}
}
\value{
The updated \code{vrpr_model}.
}
\description{
A shipment is a paired pickup and delivery: the same vehicle must pick up
the goods at the pickup point and drop them off at the delivery point,
with the pickup happening first, in the same trip. This models the classic
pickup-and-delivery problem (PDPTW when combined with time windows).
}
